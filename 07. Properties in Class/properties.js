class Person {
  constructor(name, age) {
    // we can add the properties in the constructor
    this.name = name; // this is a property of the class
    this.age = age; // this is a property of the class
  }
}
const person1 = new Person("Daffa", 20);
console.log(person1);
